# Launch app steps:

Install node modules:

- `npm install`

Start app in developer mode:

- `npm run dev`

Start app in production mode:

- `npm run build`
- `npm run start`

# Core Tech Stack

- Next.js 14
- Scss
